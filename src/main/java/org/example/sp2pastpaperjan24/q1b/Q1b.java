package org.example.sp2pastpaperjan24.q1b;
//
//Throw an exception when dealing with invalid
//input or situations that can happen in normal
//program use and must be handled even in production.

public class Q1b {
//    if (age < 0) {
//        throw new IllegalArgumentException("age must be non-negative");
//    }
//
//    Use assert for internal assumptions that should
//    always hold if the program is correct, mainly for
//    debugging.

//    assert balance >= 0;

//   Why: exceptions are part of runtime error handling;
//   assertions are mainly for catching programmer
//   mistakes and may be disabled.