package org.example.sp2pastpaperjan25.q7;


public class Q7b {

}
//A non-terminating case is any input
// with y == 0 and x >= 0, for example:

//(x, y) = (5, 0)
//
//Because the loop condition is x >= y,
// so 5 >= 0 is true, but x -= y subtracts 0 each time,
// so x never changes.
